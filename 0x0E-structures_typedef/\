#include <stdio.h>
#include "dog.h"

/**
 * print_dog - prints struct dog
 * @d: member1
 */
void print_dog(struct dog *d)
{
	if (d->owner == NULL)
		printf("(nil\n)");
	if (d->name == NULL)
		printf("Name: (nil)");
	printf("Name: %s\nAge: %6f\nOwner: %s\n, d->name, d->age, d->owner);
}

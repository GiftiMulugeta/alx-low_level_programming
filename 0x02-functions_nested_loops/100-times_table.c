#include "main.h"
#include <stdio.h>


/**
 * print_to_98 - Entry point
 * @n: The character to be print
 *
 *Return: Always 0
 */
void print_times_table(int n)
{
	int x, y;

	if (n >= 0 && n <= 15)
	{
		for (x = 0; x <= n; x++)
		{
			_putchar('0');

			for ( y = 1; y <= x; y++)
			{

		}
}
